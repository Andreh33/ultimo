/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    turbo: false
  }
};

module.exports = nextConfig;
